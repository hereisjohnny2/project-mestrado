# CPU-only build, matching plan §9 (deploy: docker compose local, CPU).
# The version pin MUST include the "+cpu" local tag: plain "torch==2.3.1"
# resolves against the default PyPI index too, and PyPI's default torch
# wheel bundles the full CUDA runtime (~5GB of nvidia-* packages) even
# though this app never uses a GPU. Pinning "+cpu" forces pip to match the
# wheel that only exists on the extra index below.
# For a GPU host, replace this pin with the matching +cuXXX version and
# point --extra-index-url at the matching CUDA index instead.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu
numpy==1.26.4
Pillow==10.3.0

fastapi==0.111.0
uvicorn[standard]==0.30.1
sqlalchemy==2.0.30
pydantic==2.7.4
pydantic-settings==2.3.4
python-multipart==0.0.9

pytest==8.2.2
httpx==0.27.0
