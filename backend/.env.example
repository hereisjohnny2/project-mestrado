# Copy to .env for local overrides (docker-compose sets its own env vars).
ROCKSEG_STORAGE_DIR=./storage
ROCKSEG_DATABASE_URL=sqlite:///./storage/db.sqlite3
ROCKSEG_CORS_ORIGINS=["http://localhost:5173"]
